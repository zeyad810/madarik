import { CurrentSubscription, PackageHistoryItem, PackagePlan } from "./types";

export const DEFAULT_PACKAGES: PackagePlan[] = [];

export const DEFAULT_CURRENT_SUBSCRIPTION: CurrentSubscription | null = null;

export const DEFAULT_PACKAGE_HISTORY: PackageHistoryItem[] = [];
