export interface NavLinkItem {
  id: string;
  label: string;
  href: string;
  allowedRoles?: string[];
}

export interface SideMenuItem {
  id: string;
  label: string;
  href: string;
  allowedRoles?: string[];
}

